# 第 9 章：分词的蝴蝶效应

> **核心命题**：分词方式决定了 LLM 处理语言的基本单位。BPE 算法、词汇表大小、特殊标记的设计，对模型处理算术、代码、非英语语言的能力有深远的"蝴蝶效应"式影响。

---

## 思维框架

你有没有发现，ChatGPT 在做简单加法时有时会出错？或者它数不清 "strawberry" 里有几个字母 r？

这不是"不够聪明"，是分词造成的。

分词（Tokenization）是 LLM 数据管道的第一步：把原始文本转换成整数序列，再送给神经网络。听起来是个小细节，但 Karpathy 为它专门录制了一个长达 2 小时的讲座。原因是：**这个"小细节"对模型能力有远比大多数人预期更深远的影响**。

举一个例子：

"strawberry" 这个词可能被分成 3 个 token：`straw` + `berry` + `（某种合并）`。或者 `st` + `raw` + `berry`。具体怎么分，取决于 BPE 算法在训练数据上学到的合并规则。

无论怎么分，模型处理的不是单个字母，而是这些 token。当你问"strawberry 里有几个字母 r？"，模型在数的是 token，而不是字符。它需要"理解"每个 token 内部的字母结构——这是额外的推理负担，而且不是模型被直接训练去做的事情。

---

## 理论基础

**什么是 BPE？**

BPE（Byte-Pair Encoding，字节对编码）是目前最主流的 LLM 分词算法，最初用于数据压缩。

算法从字节级别开始：

1. 初始词汇表 = 所有 256 个字节值
2. 在训练语料里统计所有相邻字节对的频率
3. 把最频繁的字节对合并成一个新 token，加入词汇表
4. 重复步骤 2-3，直到词汇表达到目标大小（通常 50,000 - 100,000）

结果：常见的英文词汇被合并成单个 token（"the", "and", "of"），不常见的单词被拆成子词（"uncommon" → "un" + "common"），罕见字符保持字节级别。

GPT-4 使用的 cl100k_base 分词器有 100,277 个 token。

**词汇表大小的权衡**

更大的词汇表：
- 每个 token 包含更多信息（序列更短，处理更快）
- 但嵌入矩阵更大（100k × 768 维 = 相当大的参数量）
- 罕见语言的覆盖可能不均匀

更小的词汇表：
- 序列更长（推理更慢）
- 但每个 token 更"细粒度"，更容易处理字符级别的任务

**分词造成的能力怪异性**

Karpathy 在讲座中详细讨论了几类由分词引起的 LLM 行为异常：

**算术问题**：数字被分成不可预测的 token。"1234" 可能是一个 token，"12345" 可能是两个。当你让模型做大数加法，它处理的不是数字本身，而是 token 序列，需要额外的"token 到数字"的映射。这是为什么 LLM 在大数算术上经常出错的原因之一。

**非英语语言的效率劣势**：BPE 在英语上训练时，英语单词被高效地合并成少量 token。但中文、阿拉伯语等语言，同样的信息量需要更多的 token 来表示。这意味着同样长度的上下文窗口，能放入的中文内容比英文少——非英语用户在使用 LLM 时承受着隐性的"token 效率税"。

**"SolidGoldMagikarp" 异常**：BPE 词汇表中存在一些训练数据里出现过但主模型几乎从未见过的 token。当这些 token 出现时，模型的行为完全不可预测——它的嵌入向量没有被充分训练，像是大脑里的"盲点"。

**特殊 Token 的作用**

除了普通的文本 token，分词器还定义了一系列特殊 token：

- `<|endoftext|>`：文档结束标记，告诉模型一段文本在这里结束
- `<|im_start|>` / `<|im_end|>`：标记对话中用户/助手消息的开始和结束
- `<|pad|>`：填充 token，用于批次对齐

这些特殊 token 定义了模型理解"对话格式"的方式。SFT 训练时，对话数据就是用这些 token 格式化的——模型学会了识别这些标记并按相应角色行事。

---

## 案例

> **案例："为什么 LLM 数不清字母"的实验**
>
> **情境**：Karpathy 让学生在 GPT-4 上测试"strawberry 里有几个字母 r？"
>
> **关键时刻**：他展示了这个单词的分词结果：`straw` + `berry`。这两个 token 中，"r" 分别出现在 "straw" 的第 4 位和 "berry" 的第 2 位。模型看到的不是字母序列，而是两个语义单元的序列。
>
> **结果**：要正确回答这个问题，模型需要先"拆解" token 到字母级别——这是额外的推理步骤，不在标准的训练目标中。GPT-4 答错的概率相当高。
>
> **启示**：LLM 的"愚蠢"行为往往不是推理能力不足，而是分词导致的信息表示问题。理解分词，就能预测哪类任务对模型困难。
>
> **可迁移**：当你需要 LLM 做字符级别操作（数字母、检查拼写、处理密码短语），考虑在提示词里把字符分开（比如 "s-t-r-a-w-b-e-r-r-y"），给模型提供字母级别的信息。

---

> **案例：中英文 token 效率差异实验**
>
> **情境**：Karpathy 演示了同样语义的文本，英文版和中文版在 GPT-4 分词器下的 token 数量对比
>
> **关键时刻**：一段普通的英文段落被分成 N 个 token，语义等价的中文版本需要 1.5-2 倍的 token 数量。
>
> **结果**：这意味着在同样的上下文窗口内，中文用户能放入的内容比英文用户少，使用相同的 API 需要支付更多费用。这是 BPE 在英语文本上训练所带来的系统性偏差。
>
> **启示**：分词的选择不只是技术问题，也是公平性问题。新一代多语言模型正在努力改进这个问题。
>
> **可迁移**：在需要处理大量非英语文本的项目中，评估分词效率是选择模型的重要考量之一。

---

## 在你的生活中

**把分词知识变成实用的使用技巧：**

**1. 用分词可视化工具了解你的文本**
OpenAI 提供了在线分词可视化工具（tiktokenizer.vercel.app），可以直接看到你的文本被如何分词。在任何需要精确控制 token 数量（成本估算、上下文管理）的项目里，这是必备工具。

**2. 帮助 LLM 做字符级任务**
如果你需要模型数字母、检查字符数量、处理字谜，在提示词里给它字符级别的输入格式。比如：把 "strawberry" 写成带分隔符的格式 "s-t-r-a-w-b-e-r-r-y" 或者 "{s}{t}{r}{a}{w}{b}{e}{r}{r}{y}"，成功率会显著提升。

**3. 预测哪些任务对 LLM 困难**
分词的特性让以下任务天然困难：大数算术（数字分词不均匀）、字谜游戏（字符级操作）、代码中的精确格式（空格和缩进可能被合并或拆分）。遇到这类任务，要么用工具辅助（代码解释器），要么格式化输入来帮助模型。

---

## 与下一章的连接

分词是数据进入模型的入口。从这里开始，数据经历预训练的完整流程——越来越大的模型，越来越多的数据，越来越精细的训练技术。

Karpathy 在"Let's reproduce GPT-2"项目中完整演示了这个过程：从训练技术的选择（混合精度、梯度裁剪、余弦学习率调度）到分布式训练的实现。

规模定律告诉我们：给更多数据和算力，模型性能会如何可预测地提升？

---

**本章框架一句话**：分词是 LLM 处理语言的基本单位设计——它的选择像蝴蝶效应一样影响模型在算术、多语言、代码等任务上的能力边界。
